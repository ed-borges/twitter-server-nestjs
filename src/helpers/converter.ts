// import { Tweet } from "../entity/tweet.entity";

// export const toUserDto = (data: Tweet): UserDto => {
//     const { id, username, email } = data;
//     const userDto: UserDto = { id, username, email };
//     return userDto;
//   };
